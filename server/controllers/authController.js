const express = require("express");
const modelUser = require("../models/user");

login = (req, res) => {
  res.sendStatus(200);
}

register = (req, res) => {
  res.sendStatus(200);
}

module.exports = {
  login,
  register
};